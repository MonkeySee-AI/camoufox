diff --git a/layout/style/nsMediaFeatures.cpp b/layout/style/nsMediaFeatures.cpp
index be5c5dc..f85c472 100644
--- a/layout/style/nsMediaFeatures.cpp
+++ b/layout/style/nsMediaFeatures.cpp
@@ -4,6 +4,9 @@
 
 /* the features that media queries can test */
 
+#include <string>
+
+#include "MaskConfig.hpp"
 #include "PreferenceSheet.h"
 #include "mozilla/GeckoBindings.h"
 #include "mozilla/LookAndFeel.h"
@@ -35,6 +38,23 @@ using namespace mozilla;
 using mozilla::dom::DisplayMode;
 using mozilla::dom::Document;
 
+static StyleColorGamut RotundaColorGamutFromProfileValue(
+    const std::string& aValue) {
+  if (aValue == "rec2020") {
+    return StyleColorGamut::Rec2020;
+  }
+  if (aValue == "p3") {
+    return StyleColorGamut::P3;
+  }
+  return StyleColorGamut::Srgb;
+}
+
+static StyleDynamicRange RotundaDynamicRangeFromProfileValue(
+    const std::string& aValue) {
+  return aValue == "high" ? StyleDynamicRange::High
+                           : StyleDynamicRange::Standard;
+}
+
 // A helper for four features below
 static nsSize GetSize(const Document& aDocument) {
   nsPresContext* pc = aDocument.GetPresContext();
@@ -164,6 +184,12 @@ int32_t Gecko_MediaFeatures_GetMonochromeBitsPerPixel(
 }
 
 StyleColorGamut Gecko_MediaFeatures_ColorGamut(const Document* aDocument) {
+  if (auto screen = MaskConfig::Profile().getScreen()) {
+    if (auto value = screen->getColorGamut()) {
+      return RotundaColorGamutFromProfileValue(*value);
+    }
+  }
+
   auto* dx = GetDeviceContextFor(aDocument);
   if (!dx || aDocument->ShouldResistFingerprinting(RFPTarget::CSSColorInfo)) {
     return StyleColorGamut::Srgb;
@@ -185,6 +211,17 @@ int32_t Gecko_MediaFeatures_GetColorDepth(const Document* aDocument) {
     return 0;
   }
 
+  if (auto screen = MaskConfig::Profile().getScreen()) {
+    if (auto colorDepth = screen->getColorDepth()) {
+      return *colorDepth / 3;
+    }
+    // Keep CSS `color` coherent when only pixelDepth is supplied by a
+    // fingerprint profile.
+    if (auto pixelDepth = screen->getPixelDepth()) {
+      return *pixelDepth / 3;
+    }
+  }
+
   // Use depth of 24 when resisting fingerprinting, or when we're not being
   // rendered.
   int32_t depth = 24;
@@ -389,6 +426,12 @@ StyleScripting Gecko_MediaFeatures_Scripting(const Document* aDocument) {
 }
 
 StyleDynamicRange Gecko_MediaFeatures_DynamicRange(const Document* aDocument) {
+  if (auto screen = MaskConfig::Profile().getScreen()) {
+    if (auto value = screen->getDynamicRange()) {
+      return RotundaDynamicRangeFromProfileValue(*value);
+    }
+  }
+
   // Bug 1759772: Once HDR color is available, update each platform
   // LookAndFeel implementation to return StyleDynamicRange::High when
   // appropriate.
@@ -397,6 +440,12 @@ StyleDynamicRange Gecko_MediaFeatures_DynamicRange(const Document* aDocument) {
 
 StyleDynamicRange Gecko_MediaFeatures_VideoDynamicRange(
     const Document* aDocument) {
+  if (auto screen = MaskConfig::Profile().getScreen()) {
+    if (auto value = screen->getVideoDynamicRange()) {
+      return RotundaDynamicRangeFromProfileValue(*value);
+    }
+  }
+
   if (aDocument->ShouldResistFingerprinting(RFPTarget::CSSVideoDynamicRange)) {
     return StyleDynamicRange::Standard;
   }
